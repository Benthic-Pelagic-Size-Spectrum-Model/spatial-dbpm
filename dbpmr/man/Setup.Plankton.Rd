% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Setup.plankton.r
\name{Setup.Plankton}
\alias{Setup.Plankton}
\title{Set up the plankton resource}
\usage{
Setup.Plankton(
  run.in,
  mmin = -12 * log(10),
  mmax = -3 * log(10),
  mu_0 = 0.2,
  beta = -0.25,
  u_0 = 0.01,
  lambda = -1,
  initial_flag = FALSE,
  ts_flag = FALSE,
  filename
)
}
\arguments{
\item{run.in}{A \link{run.params} object.}

\item{mmin, mmax}{Minimum and maximum log-mass of the plankton spectrum. The
defaults span the canonical FishMIP plankton range \code{10^-12} to \code{10^-3} g
(natural log, \code{-12*log(10)} to \code{-3*log(10)}), meeting the consumer spectrum
at \code{10^-3} g.}

\item{mu_0}{Mortality constant.}

\item{beta}{Mortality mass-scaling exponent.}

\item{u_0}{Abundance constant.}

\item{lambda}{Spectrum slope.}

\item{initial_flag, ts_flag}{Logical flags indicating whether initial-condition
and time-series input files are supplied (see \code{\link[=Setup.ts]{Setup.ts()}}).}

\item{filename}{Character string naming the species (and its output
directory). Required.}
}
\value{
A \link{plankton.params} object.
}
\description{
Builds a \link{plankton.params} object and creates the species output directory
under the run directory.
}
