% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Setup.Pelagic.r
\name{Setup.Pelagic}
\alias{Setup.Pelagic}
\title{Set up a pelagic species}
\usage{
Setup.Pelagic(
  run.in,
  mmin = -3 * log(10),
  mmat = 2 * log(10),
  mmax = 6 * log(10),
  A = 640,
  alpha = 0.82,
  mu_0 = 0.2,
  beta = -0.25,
  mu_s = 0.1,
  epsilon = 0.1,
  u_0 = 0.01,
  lambda = -1,
  K_pla,
  R_pla,
  Ex_pla,
  K_pel = 0.2,
  R_pel = 0.2,
  Ex_pel = 0.3,
  K_ben = 0.1,
  R_ben = 0.2,
  Ex_ben = 0.4,
  pref_pla = 1,
  pref_pel = 1,
  pref_ben = 1,
  q_0 = log(100),
  sig = log(10),
  trunc = 2,
  prey = 0,
  pred = 0,
  comp = 0.1,
  gamma_prey = 0.33,
  gamma_pred = 0.33,
  gamma_comp = 0.75,
  rep_method = 2,
  initial_flag = FALSE,
  ts_flag = FALSE,
  fishing_flag = FALSE,
  filename
)
}
\arguments{
\item{run.in}{A \link{run.params} object.}

\item{mmin, mmat, mmax}{Minimum, maturation and maximum log-mass of the species.
The defaults are the canonical FishMIP consumer range \code{10^-3} to \code{10^6} g
(natural log, \code{-3*log(10)} to \code{6*log(10)}), so the consumer minimum is
\code{10^-3} g.}

\item{A}{Search/encounter rate constant.}

\item{alpha}{Search-rate mass-scaling exponent.}

\item{mu_0}{Background mortality constant.}

\item{beta}{Mortality mass-scaling exponent.}

\item{mu_s}{Senescence mortality constant.}

\item{epsilon}{Senescence size offset: added to the maximum log10 size in the
senescence denominator
\verb{mu_s * (log10 w - log10 w_min) / ((log10 w_max + epsilon) - log10 w)}, so
senescence mortality stays finite at \code{w_max}. This is \strong{not} a growth
efficiency (the growth efficiencies are \code{K_pla}/\code{K_pel}/\code{K_ben}).}

\item{u_0}{Abundance constant.}

\item{lambda}{Spectrum slope.}

\item{K_pla, R_pla, Ex_pla}{Growth (assimilation-to-growth), reproduction
(retention) and excretion fractions of intake when feeding on plankton.
Together with defecation they partition each unit of intake, so
\code{K + R + Ex = 1 - defecation}. Default to the corresponding pelagic values.}

\item{K_pel, R_pel, Ex_pel}{Growth, reproduction and excretion fractions for
feeding on other pelagics.}

\item{K_ben, R_ben, Ex_ben}{Growth, reproduction and excretion fractions for
feeding on benthos.}

\item{pref_pla, pref_pel, pref_ben}{Feeding preferences for plankton, pelagic
and benthic prey.}

\item{q_0}{Preferred predator-prey log-mass ratio.}

\item{sig}{Width of the feeding kernel.}

\item{trunc}{Truncation of the feeding kernel.}

\item{prey, pred, comp}{Density-dependence coefficients for prey, predator and
competition effects.}

\item{gamma_prey, gamma_pred, gamma_comp}{Mass-scaling exponents for the prey,
predator and competition effects.}

\item{rep_method}{Integer reproduction method. \code{0} = fixed recruitment held
at the initial density of the smallest size class; \code{1} = a prescribed
reproduction time series read from an input file (see \code{\link[=Setup.Rep]{Setup.Rep()}});
\code{2} (the default) = reproduction allocated from assimilated energy of
mature individuals; \code{3} = reproduction proportional to mature biomass.
Methods \code{0}, \code{2} and \code{3} require no input file.}

\item{initial_flag, ts_flag, fishing_flag}{Logical flags indicating whether
initial-condition, time-series and fishing input files are supplied.}

\item{filename}{Character string naming the species (and its output
directory). Required.}
}
\value{
A \link{pelagic.params} object.
}
\description{
Builds a \link{pelagic.params} object and creates the species output directory
under the run directory.
}
\note{
\code{rep_method = 1} requires a reproduction time-series input file (see
\code{\link[=Setup.Rep]{Setup.Rep()}}). These input data must be supplied by the user and are
\strong{not} provided with the package; without the file \code{\link[=SizeSpectrum]{SizeSpectrum()}} stops
with an error. The other methods (\code{0}, \code{2}, \code{3}) need no input file.
}
\seealso{
\code{\link[=Setup.Benthic]{Setup.Benthic()}}, \code{\link[=Setup.Rep]{Setup.Rep()}}, \code{\link[=Setup.fishing]{Setup.fishing()}}
}
